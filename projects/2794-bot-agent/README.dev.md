Development notes

1) Install deps: npm install
2) Copy .env.example to .env and fill LLM_API_KEY (and PLATFORM_API_KEY for real posts)
3) Run generate sample: npm run generate -- --topic "short topic here"
4) Run post (dry-run): npm run post -- --topic "topic here" --dry-run
